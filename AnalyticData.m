classdef (ClassAttributes) AnalyticData
%解析方法求解R，CSV
    properties
        R
        csv
    end
    methods
        function obj = calculateR0(LocalData)
        %calculateR0 - The analytic method calculates the value of R
            LocalData.
        end
    end
end